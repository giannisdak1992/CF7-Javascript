/**
 * JavaScript uses a dynammic policy, no datatypes needed.
 * Variables are camelCase
 * JS is case-sensitive (PI not the same to Pi and pI)
 * Avoid declaring with var.
 * take care of syntax. Spaces between operators and operands
 */

const PI = 3.14 //Declares 'true' constants
const maxThreads = 150 //Declares a 'contextual' variable
let counter = 0 //Declares non - constant
counter++

console.log(PI, typeof PI, maxThreads, counter)
